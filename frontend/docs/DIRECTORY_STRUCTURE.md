# フロントエンド ディレクトリ構成

このドキュメントでは、フロントエンド（Next.js App Router）のディレクトリ構成について説明します。

## 概要

```
frontend/src/
├── app/              # Next.js App Router ページ
├── components/       # React コンポーネント
├── hooks/            # カスタムフック
├── lib/              # ライブラリ・ユーティリティ
├── providers/        # React Context プロバイダー
├── schemas/          # Zod バリデーションスキーマ
├── stores/           # Zustand ストア
├── types/            # TypeScript 型定義
├── utils/            # ユーティリティ関数
└── __tests__/        # テストファイル
```

---

## 📁 app/ - ページ・ルーティング

Next.js App Router によるファイルベースルーティング。

```
app/
├── layout.tsx                # ルートレイアウト
├── page.tsx                  # トップページ (/)
├── globals.css               # グローバルスタイル
├── manifest.js               # PWA マニフェスト
├── apple-icon.js             # Apple アイコン生成
│
├── login/page.tsx            # ログイン (/login)
├── register/page.tsx         # ユーザー登録 (/register)
├── forgot-password/page.tsx  # パスワード忘れ (/forgot-password)
├── reset-password/page.tsx   # パスワードリセット (/reset-password)
│
├── mypage/page.tsx           # マイページ (/mypage)
│
├── search/                   # 検索機能
│   ├── match/                # 試合検索 (/search/match)
│   └── team/                 # チーム検索 (/search/team)
│
├── sumdownload/              # 一括ダウンロード
│   ├── SumDownloadSearchBase.tsx  # 共通コンポーネント
│   ├── match/                # 試合一括DL (/sumdownload/match)
│   └── team/                 # チーム一括DL (/sumdownload/team)
│
├── upload/                   # アップロード機能
│   ├── page.tsx              # アップロードトップ (/upload)
│   └── match/                # 試合アップロード (/upload/match)
│
├── event/page.tsx            # イベント (/event)
│
└── [静的ページ]
    ├── about/page.tsx            # サイト概要 (/about)
    ├── guide/page.tsx            # 使い方ガイド (/guide)
    ├── faq/page.tsx              # FAQ (/faq)
    ├── info/page.tsx             # お知らせ (/info)
    ├── external-links/page.tsx   # 外部リンク (/external-links)
    ├── privacy-policy/page.tsx   # プライバシーポリシー
    └── terms-of-service/page.tsx # 利用規約
```

---

## 📁 components/ - コンポーネント

### ルート直下（共通コンポーネント）

```
components/
├── Header.tsx          # ヘッダー
├── HeaderLogo.tsx      # ヘッダーロゴ
├── HeaderNav.tsx       # ヘッダーナビゲーション
├── Footer.tsx          # フッター
├── FooterSection.tsx   # フッターセクション
├── SideMenu.tsx        # サイドメニュー
├── Calendar.tsx        # カレンダー
├── DeleteModal.tsx     # 削除確認モーダル
├── ErrorBoundary.tsx   # エラーバウンダリ
├── Icons.tsx           # アイコン定義
├── ShareButtons.tsx    # SNSシェアボタン
│
└── [トップページ用セクション]
    ├── HeroSection.tsx       # ヒーローセクション
    ├── FeaturesSection.tsx   # 機能紹介
    ├── SearchSection.tsx     # 検索セクション
    ├── DownloadSection.tsx   # ダウンロードセクション
    ├── UploadSection.tsx     # アップロードセクション
    └── JoinNetworkSection.tsx # CTA セクション
```

### auth/ - 認証関連

```
components/auth/
├── AuthGuard.tsx         # 認証ガード（ルート保護）
├── LoginForm.tsx         # ログインフォーム
├── RegisterForm.tsx      # 登録フォーム
├── ForgotPasswordForm.tsx    # パスワード忘れフォーム
└── PasswordResetForm.tsx     # パスワードリセットフォーム
```

### events/ - イベント関連

```
components/events/
├── EventRegistrationForm.tsx    # イベント登録フォーム
├── EventRegistrationNotice.tsx  # 登録案内
├── EventConfirmDialog.tsx       # 確認ダイアログ
└── EventTypeDisplay.tsx         # イベント種別表示
```

### search/ - 検索機能

```
components/search/
├── SearchForm.tsx        # 検索フォーム
├── SearchHeader.tsx      # 検索ヘッダー
├── SearchResults.tsx     # 検索結果
├── SearchAutocomplete.tsx # オートコンプリート
├── MatchCards.tsx        # 試合カード表示
├── TeamCards.tsx         # チームカード表示
├── TeamTable.tsx         # チームテーブル表示
├── Pagination.tsx        # ページネーション
├── QuickFilters.tsx      # クイックフィルター
├── TagCloud.tsx          # タグクラウド
├── ViewToggleButton.tsx  # 表示切替ボタン
├── DesktopSidebar.tsx    # PC用サイドバー
├── MobileSidebar.tsx     # モバイル用サイドバー
├── SidebarItem.tsx       # サイドバーアイテム
└── mockData.ts           # モックデータ
```

### mypage/ - マイページ

```
components/mypage/
├── ProfileSection.tsx         # プロフィール
├── FileListSection.tsx        # ファイル一覧
├── UploadedMatchesSection.tsx # アップロード済み試合
├── UploadedTeamsSection.tsx   # アップロード済みチーム
└── RegisteredEventsSection.tsx # 登録イベント一覧
```

### upload/ - アップロード

```
components/upload/
├── FileUploadForm.tsx    # アップロードフォーム
├── FileDropZone.tsx      # ドラッグ&ドロップエリア
└── UploadProgress.tsx    # アップロード進捗
```

### layout/ - レイアウト

```
components/layout/
├── index.ts              # エクスポート
├── page-layout.tsx       # ページレイアウト
├── responsive.tsx        # レスポンシブユーティリティ
├── focus-manager.tsx     # フォーカス管理
├── StaticPageLayout.tsx  # 静的ページレイアウト
└── StaticPageContent.tsx # 静的ページコンテンツ
```

### ui/ - 汎用UIコンポーネント

shadcn/ui ベースの再利用可能なUIコンポーネント。

```
components/ui/
├── index.ts          # エクスポート
├── button.tsx        # ボタン
├── input.tsx         # 入力フィールド
├── textarea.tsx      # テキストエリア
├── select.tsx        # セレクトボックス
├── checkbox.tsx      # チェックボックス
├── label.tsx         # ラベル
├── form.tsx          # フォーム
├── card.tsx          # カード
├── badge.tsx         # バッジ
├── table.tsx         # テーブル
├── tabs.tsx          # タブ
├── dialog.tsx        # ダイアログ
├── modal.tsx         # モーダル
├── dropdown.tsx      # ドロップダウン
├── pagination.tsx    # ページネーション
├── date-picker.tsx   # 日付ピッカー
├── datetime-input.tsx # 日時入力
├── toast.tsx         # トースト通知
├── loading.tsx       # ローディング
├── skeleton.tsx      # スケルトン
└── progress.tsx      # プログレスバー
```

---

## 📁 hooks/ - カスタムフック

```
hooks/
├── useAuth.ts              # 認証状態管理
├── usePasswordReset.ts     # パスワードリセット
├── useSearch.ts            # 検索機能
├── useSearchSuggestions.ts # 検索サジェスト
├── useAutocomplete.ts      # オートコンプリート
├── useFileUpload.ts        # ファイルアップロード
├── useEventRegistration.ts # イベント登録
├── useToast.ts             # トースト通知
├── useDebounce.ts          # デバウンス
├── useErrorHandler.ts      # エラーハンドリング
├── useViewMode.ts          # 表示モード切替
├── useSumDownloadManager.ts # 一括ダウンロード管理
│
└── api/                    # API関連フック
    ├── useMyPage.ts        # マイページAPI
    └── useSumDownload.ts   # 一括ダウンロードAPI
```

---

## 📁 lib/ - ライブラリ

```
lib/
├── utils.ts            # 汎用ユーティリティ (cn関数等)
├── logout.ts           # ログアウト処理
│
└── api/                # APIクライアント
    ├── client.ts       # Axios クライアント設定
    ├── utils.ts        # API ユーティリティ
    ├── auth.ts         # 認証API
    ├── search.ts       # 検索API
    ├── files.ts        # ファイルAPI
    ├── upload.ts       # アップロードAPI
    ├── mypage.ts       # マイページAPI
    ├── events.ts       # イベントAPI
    └── sumdownload.ts  # 一括ダウンロードAPI
```

---

## 📁 providers/ - プロバイダー

```
providers/
├── index.ts              # 統合プロバイダー
├── query-provider.tsx    # TanStack Query プロバイダー
└── toast-provider.tsx    # トースト通知プロバイダー
```

---

## 📁 schemas/ - バリデーションスキーマ

Zod によるバリデーションスキーマ定義。

```
schemas/
├── index.ts          # エクスポート
├── auth.ts           # 認証関連スキーマ
├── event.ts          # イベントスキーマ
├── file.ts           # ファイルスキーマ
└── sumdownload.ts    # 一括ダウンロードスキーマ
```

---

## 📁 stores/ - 状態管理

Zustand によるグローバル状態管理。

```
stores/
├── authStore.ts      # 認証状態ストア
└── myPageStore.ts    # マイページ状態ストア
```

---

## 📁 types/ - 型定義

```
types/
├── index.ts          # エクスポート
├── api.ts            # API レスポンス型
├── user.ts           # ユーザー型
├── file.ts           # ファイル型
├── event.ts          # イベント型
├── search.ts         # 検索型
├── form.ts           # フォーム型
├── ui.ts             # UI型
└── utils.ts          # ユーティリティ型
```

---

## 📁 utils/ - ユーティリティ関数

```
utils/
├── api.ts              # API ユーティリティ
├── apiErrorHandler.ts  # APIエラーハンドリング
├── dateFormatters.ts   # 日付フォーマット
└── transformers.ts     # データ変換
```

---

## 📁 __tests__/ - テスト

テストファイルは `src/` 配下の構造をミラーリング。

```
__tests__/
├── setup.ts              # テストセットアップ
├── mocks/                # MSW モックハンドラー
│   ├── handlers.ts
│   └── server.ts
├── utils/test-utils.tsx  # テストユーティリティ
│
├── components/           # コンポーネントテスト
│   ├── *.test.tsx
│   ├── auth/
│   ├── events/
│   ├── features/
│   ├── layout/
│   ├── mypage/
│   ├── search/
│   ├── ui/
│   └── upload/
│
├── hooks/                # フックテスト
│   ├── *.test.ts
│   └── api/
│
├── lib/                  # ライブラリテスト
│   └── api/
│
├── providers/            # プロバイダーテスト
├── schemas/              # スキーマテスト
├── stores/               # ストアテスト
├── types/                # 型テスト
├── utils/                # ユーティリティテスト
└── scripts/              # スクリプトテスト
```

---

## 設計原則

### 1. 機能ベースのディレクトリ構成

コンポーネントは機能単位でグループ化（`auth/`, `search/`, `mypage/` 等）。

### 2. 関心の分離

- **components/**: UI表示ロジック
- **hooks/**: 状態・副作用ロジック
- **lib/api/**: API通信
- **stores/**: グローバル状態
- **schemas/**: バリデーション
- **types/**: 型定義

### 3. 再利用性

- `components/ui/`: shadcn/ui ベースの汎用コンポーネント
- `hooks/`: 再利用可能なロジック
- `utils/`: 汎用ユーティリティ関数

### 4. テスト構造

テストファイルは実装ファイルの構造をミラーリングし、対応関係を明確化。
